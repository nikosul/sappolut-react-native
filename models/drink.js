class Drink {
  constructor(id, title, brewery, rating, imageUri) {
    this.id = id;
    this.title = title;
    this.brewery = brewery;
    this.rating = rating;
    this.imageUri = imageUri;
  }
}
export default Drink;